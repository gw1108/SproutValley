# Suggested Features

Ideas noticed during development that are NOT in the design documents. Each needs
developer sign-off (and a GDD entry) before implementation.

- **Settings menu content** (2026-07-18): the HUD now has a settings button (new
  `ui-settings-icon` art), but no settings exist in the GDD, so it opens a
  placeholder "Nothing to configure yet" panel. Candidates when designed:
  music/SFX volume sliders (Kenney `slide_*` assets already cataloged),
  fullscreen toggle.
